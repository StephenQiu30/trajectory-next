# 构建产物 & 依赖 (必须排除)
.next
node_modules

# 版本控制
.git
.gitignore

# 开发环境变量 (排除开发配置，保留 .env.production)
.env
.env*.local

# 开发工具配置
.prettier*
.eslint*

# Docker 自身文件 (避免递归)
docker-compose.yml
Dockerfile
.dockerignore

# 文档 (构建不需要)
*.md
